// RECOMMENDED FIRESTORE SECURITY RULES FOR PRODUCTION
// Copy these to: Firebase Console → Firestore Database → Rules

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // Option 1: Restrict to your GitHub Pages domain only
      allow read, write: if request.host.matches('logic06183\\.github\\.io');
      
      // Option 2: Require authentication (most secure)
      // allow read, write: if request.auth != null;
      
      // Option 3: Read-only public access (good for display dashboards)
      // allow read: if true;
      // allow write: if request.auth != null;
    }
  }
}
